<Window x:Class="CapaPresentacion.FormConsumoPorVenta"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Consumo de Materias Primas por Venta" Height="600" Width="900"
        Background="#fff8e1" FontFamily="Georgia"
        WindowStartupLocation="CenterScreen">

    <Border Background="White" CornerRadius="12" Padding="20"
            BorderBrush="#e2a42d" BorderThickness="2" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="CONSUMO POR VENTA" FontSize="24" FontWeight="Bold"
                       Foreground="#a36b00" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                <TextBlock Text="Buscar por ID de Venta:" VerticalAlignment="Center" Margin="0,0,10,0"
                           FontWeight="Bold" Foreground="#5c3a00"/>
                <TextBox x:Name="txtIdVenta" Width="150"/>
                <Button x:Name="btnConsultar" Content="Consultar" Width="120" Margin="10,0,0,0"
                        Background="#e2a42d" Foreground="White" FontWeight="Bold"
                        Click="btnConsultar_Click"/>
            </StackPanel>

            <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="5"
                    BorderBrush="#e2a42d" BorderThickness="2">
                <DataGrid x:Name="dgvConsumoVenta" AutoGenerateColumns="False" IsReadOnly="True"
                          Background="White" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Materia Prima" Binding="{Binding MateriaPrima}" Width="*"/>
                        <DataGridTextColumn Header="Cantidad Consumida" Binding="{Binding CantidadConsumida}" Width="200"/>
                        <DataGridTextColumn Header="Unidad" Binding="{Binding Unidad}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Border>
</Window>