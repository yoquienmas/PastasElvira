<Window x:Class="CapaPresentacion.FormProducto"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Formulario de Producto" Height="750" Width="1270"
        Background="#fff8e1"
        FontFamily="Georgia"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">

    <Border Background="White" CornerRadius="12" Padding="20"
            BorderBrush="#e2a42d" BorderThickness="2" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="🍝 GESTIÓN DE PRODUCTOS" FontSize="24" FontWeight="Bold"
                       Foreground="#a36b00" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <GroupBox Header="INGRESO Y EDICIÓN DE PRODUCTO" Grid.Row="1" FontWeight="Bold" Foreground="#a36b00">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Nombre:" Grid.Row="0" Grid.Column="0" Margin="0,5,5,5" VerticalAlignment="Center" Foreground="#5c3a00"/>
                    <TextBox x:Name="txtNombre" Grid.Row="0" Grid.Column="1" Margin="0,5,10,5"/>

                    <TextBlock Text="Tipo:" Grid.Row="0" Grid.Column="2" Margin="0,5,5,5" VerticalAlignment="Center" Foreground="#5c3a00"/>
                    <TextBox x:Name="txtTipo" Grid.Row="0" Grid.Column="3" Margin="0,5,10,5"/>

                    <TextBlock Text="Costo Producción:" Grid.Row="0" Grid.Column="4" Margin="0,5,5,5" VerticalAlignment="Center" Foreground="#5c3a00"/>
                    <TextBox x:Name="txtCostoProduccion" Grid.Row="0" Grid.Column="5" Margin="0,5,0,5"/>

                    <TextBlock Text="Margen Ganancia (%):" Grid.Row="1" Grid.Column="0" Margin="0,5,5,5" VerticalAlignment="Center" Foreground="#5c3a00"/>
                    <TextBox x:Name="txtMargenGanancia" Grid.Row="1" Grid.Column="1" Margin="0,5,10,5"/>

                    <TextBlock Text="Precio Venta:" Grid.Row="1" Grid.Column="2" Margin="0,5,5,5" VerticalAlignment="Center" Foreground="#5c3a00"/>
                    <TextBox x:Name="txtPrecio" Grid.Row="1" Grid.Column="3" Margin="0,5,10,5"/>

                    <TextBlock Text="Stock Actual:" Grid.Row="1" Grid.Column="4" Margin="0,5,5,5" VerticalAlignment="Center" Foreground="#5c3a00"/>
                    <TextBox x:Name="txtStock" Grid.Row="1" Grid.Column="5" Margin="0,5,0,5"/>

                    <TextBlock Text="Stock Mínimo:" Grid.Row="2" Grid.Column="0" Margin="0,5,5,5" VerticalAlignment="Center" Foreground="#5c3a00"/>
                    <TextBox x:Name="txtStockMinimo" Grid.Row="2" Grid.Column="1" Margin="0,5,10,5"/>

                    <CheckBox x:Name="chkVisible" Content="Producto Visible" Grid.Row="2" Grid.Column="2" IsChecked="True" Foreground="#5c3a00" Margin="0,5,0,5" VerticalAlignment="Center"/>

                    <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
                        <Button x:Name="btnAgregar" Content="➕ Agregar" Width="120" Height="35" Margin="0,0,10,0"
                                Background="#4CAF50" Foreground="White" FontWeight="Bold"
                                Click="btnAgregar_Click"/>
                        <Button x:Name="btnEditar" Content="✏️ Editar" Width="120" Height="35" Margin="0,0,10,0"
                                Background="#FF9800" Foreground="White" FontWeight="Bold"
                                Click="btnEditar_Click"/>
                        <Button x:Name="btnEliminar" Content="🗑️ Eliminar" Width="120" Height="35" Margin="0,0,10,0"
                                Background="#f44336" Foreground="White" FontWeight="Bold"
                                Click="btnEliminar_Click"/>
                        <Button x:Name="btnLimpiar" Content="Limpiar" Width="120" Height="35"
                                Background="#6c757d" Foreground="White" FontWeight="Bold"
                                Click="btnLimpiar_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <Border Grid.Row="2" Margin="0,20,0,0" Background="White" CornerRadius="10" Padding="5"
                    BorderBrush="#e2a42d" BorderThickness="2">
                <DataGrid x:Name="dgvProductos" AutoGenerateColumns="False" IsReadOnly="True"
                          SelectionMode="Single" SelectionChanged="dgvProductos_SelectionChanged"
                          Background="White" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding IdProducto}" Width="60"/>
                        <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="120"/>
                        <DataGridTextColumn Header="Costo" Binding="{Binding CostoProduccion, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="Margen (%)" Binding="{Binding MargenGanancia}" Width="100"/>
                        <DataGridTextColumn Header="Precio" Binding="{Binding PrecioVenta, StringFormat=C}" Width="120"/>
                        <DataGridTextColumn Header="Stock" Binding="{Binding StockActual}" Width="80"/>
                        <DataGridTextColumn Header="Stock Min" Binding="{Binding StockMinimo}" Width="100"/>
                        <DataGridCheckBoxColumn Header="Visible" Binding="{Binding Visible}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Border>
</Window>