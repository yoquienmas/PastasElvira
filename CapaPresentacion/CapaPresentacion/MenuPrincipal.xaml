<Window x:Class="CapaPresentacion.MenuPrincipal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Menú Principal - Pastas Elvira" Height="397" Width="751"
        Background="#fff8e1" FontFamily="Georgia">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="1" Grid.Column="1" Text="Menú Principal" FontSize="25" FontWeight="Bold" Foreground="#a36b00"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Center">
            <Button Content="Gestionar Productos" Width="200" Height="40" Background="#e2a42d" Foreground="White"
                    FontWeight="Bold" Click="btnProductos_Click" Margin="0,10,0,10"/>
            <Button Content="Gestionar Materias Primas" Width="200" Height="40" Background="#e2a42d" Foreground="White"
                    FontWeight="Bold" Click="btnMateriasPrimas_Click" Margin="0,10,0,10"/>
            <Button Content="Gestionar Materias Primas" Width="200" Height="40" Background="#e2a42d" Foreground="White"
        FontWeight="Bold" Click="btnVentas_Click" Margin="0,10,0,10"/>

            <Button Content="Salir" Width="200" Height="40" Background="#ff6b6b" Foreground="White"
                    FontWeight="Bold" Click="btnSalir_Click" Margin="0,10,0,10"/>
        </StackPanel>
    </Grid>
</Window>