<Window x:Class="CapaPresentacion.FormVenta"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CapaPresentacion"
        mc:Ignorable="d"
        Title="Registro de Venta - Pastas Elvira" Height="600" Width="800"
        Background="#fff8e1" FontFamily="Georgia"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Título del Formulario -->
        <TextBlock Text="REGISTRO DE VENTA" FontSize="24" FontWeight="Bold" Foreground="#a36b00"
                   HorizontalAlignment="Center" Margin="0,10,0,20" Grid.Row="0"/>

        <!-- Sección para agregar productos a la venta -->
        <Border Grid.Row="1" BorderBrush="#e2a42d" BorderThickness="2" CornerRadius="5" Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel Margin="0,0,20,0">
                    <Label Content="Seleccionar Producto:" FontWeight="Bold" Foreground="#a36b00"/>
                    <ComboBox x:Name="cboProductos" Width="250" Height="30" Padding="5"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <Label Content="Cantidad:" FontWeight="Bold" Foreground="#a36b00"/>
                    <TextBox x:Name="txtCantidad" Width="80" Height="30" Padding="5" Text="1"/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <Label Content="Precio Unitario:" FontWeight="Bold" Foreground="#a36b00"/>
                    <!-- Este TextBox se llenará automáticamente al seleccionar un producto -->
                    <TextBox x:Name="txtPrecioUnitario" Width="100" Height="30" Padding="5" IsReadOnly="True" Background="#f0f0f0"/>
                </StackPanel>

                <StackPanel VerticalAlignment="Bottom">
                    <Button x:Name="btnAgregar" Content="Agregar Producto" Width="150" Height="30" Background="#4CAF50" Foreground="White" FontWeight="Bold" Padding="5"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Sección de DataGrid para mostrar los ítems de la venta -->
        <DataGrid x:Name="dgvItemsVenta" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  Margin="0,10" Background="White" BorderBrush="#e2a42d" BorderThickness="2">
            <DataGrid.Columns>
                <!-- Se mostrarán los ítems de la venta aquí -->
                <DataGridTextColumn Header="Producto" Binding="{Binding Producto.Nombre}" Width="*"/>
                <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="*"/>
                <DataGridTextColumn Header="Precio Unitario" Binding="{Binding PrecioUnitario}" Width="*"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Sección para el total y los botones de acción -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                <Label Content="TOTAL VENTA:" FontSize="16" FontWeight="Bold" Foreground="#a36b00"/>
                <TextBlock x:Name="txtTotalVenta" FontSize="16" FontWeight="Bold" Foreground="#a36b00" Text="0.00"/>
            </StackPanel>

            <Button x:Name="btnEliminar" Content="Eliminar Ítem" Width="130" Height="30" Background="#ff6b6b" Foreground="White" FontWeight="Bold" Margin="0,0,10,0"/>
            <Button x:Name="btnRegistrarVenta" Content="Registrar Venta" Width="150" Height="30" Background="#e2a42d" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

    </Grid>
</Window>
