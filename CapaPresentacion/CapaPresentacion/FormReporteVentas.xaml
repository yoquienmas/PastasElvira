<Window x:Class="CapaPresentacion.FormReporteVentas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reporte de Ventas por Fecha" Height="600" Width="1000"
        Background="#fff8e1"
        FontFamily="Georgia"
        WindowStartupLocation="CenterScreen">

    <Border Background="White" CornerRadius="12" Padding="20"
            BorderBrush="#e2a42d" BorderThickness="2" Margin="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="📈 REPORTE DE VENTAS POR FECHA" FontSize="24" FontWeight="Bold"
                       Foreground="#a36b00" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <GroupBox Header="FILTRAR POR FECHA" Grid.Row="1" FontWeight="Bold" Foreground="#a36b00">
                <StackPanel Orientation="Horizontal" Margin="10">
                    <TextBlock Text="Fecha Inicio:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#5c3a00"/>
                    <DatePicker x:Name="dtpFechaInicio" Width="150" Margin="0,0,20,0"/>

                    <TextBlock Text="Fecha Fin:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#5c3a00"/>
                    <DatePicker x:Name="dtpFechaFin" Width="150" Margin="0,0,20,0"/>

                    <Button x:Name="btnConsultar" Content="Consultar" Width="120"
                            Background="#e2a42d" Foreground="White" FontWeight="Bold"
                            Click="btnConsultar_Click"/>
                </StackPanel>
            </GroupBox>

            <Border Grid.Row="2" Margin="0,20,0,0" Background="White" CornerRadius="10" Padding="5"
                    BorderBrush="#e2a42d" BorderThickness="2">
                <DataGrid x:Name="dgvReporteVentas" AutoGenerateColumns="False" IsReadOnly="True"
                          Background="White" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID Venta" Binding="{Binding IdVenta}" Width="80"/>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" Width="160"/>
                        <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="*"/>
                        <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="150"/>
                        <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Border>
</Window>