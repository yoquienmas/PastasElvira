<Window x:Class="CapaPresentacion.MenuVendedor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CapaPresentacion"
        mc:Ignorable="d"
        Title="Menu Vendedor" Height="450" Width="800"
        Background="#fff8e1"
        FontFamily="Georgia"
        WindowStartupLocation="CenterScreen">
    <Grid>
        
        <Border Background="White" CornerRadius="12" Padding="20"
                BorderBrush="#e2a42d" BorderThickness="2" Margin="20">
            
            <StackPanel>
                <Button Content="🚪 Cerrar Sesión" 
            Background="#6c757d" Foreground="White" FontWeight="Bold"
            Padding="10,5" Click="BtnCerrarSesion_Click"
            Cursor="Hand"/>
                <TextBlock Text="MENÚ VENDEDOR" FontSize="24" FontWeight="Bold" Foreground="#a36b00"
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <WrapPanel Margin="0,0,0,20">
                    <Button Content="➕ NUEVA VENTA" Width="180" Height="60" Margin="5"
                            Background="#4CAF50" Foreground="White" FontWeight="Bold" FontSize="14"
                            Click="btnNuevaVenta_Click"/>
                    <Button Content="Mis Ventas" Width="180" Height="60" Margin="5"
                            Background="#FF9800" Foreground="White" FontWeight="Bold" FontSize="14"
                            Click="btnMisVentas_Click"/>
                    <Button Content="👥 GESTIÓN CLIENTES" Width="180" Height="60" Margin="5"
                            Background="#2196F3" Foreground="White" FontWeight="Bold" FontSize="14"
                            Click="btnGestionClientes_Click"/>
                </WrapPanel>

                <GroupBox Header="💰 MIS VENTAS DEL MES" Margin="0,0,0,20" FontWeight="Bold" Foreground="#a36b00">
                    <StackPanel Margin="10">
                        <WrapPanel Margin="0,0,0,10">
                            <Border Background="#e8f5e8" CornerRadius="5" Padding="10" Margin="5">
                                <TextBlock>
                                    <Run Text="Total Vendido: " FontWeight="Bold"/>
                                    <Run x:Name="txtMisVentasTotal" Text="0" Foreground="#2e7d32"/>
                                </TextBlock>
                            </Border>
                            <Border Background="#fff3e0" CornerRadius="5" Padding="10" Margin="5">
                                <TextBlock>
                                    <Run Text="Ventas Realizadas: " FontWeight="Bold"/>
                                    <Run x:Name="txtMisVentasCantidad" Text="0" Foreground="#f57c00"/>
                                </TextBlock>
                            </Border>
                            <Border Background="#e3f2fd" CornerRadius="5" Padding="10" Margin="5">
                                <TextBlock>
                                    <Run Text="Ticket Promedio: " FontWeight="Bold"/>
                                    <Run x:Name="txtMisTicketPromedio" Text="0" Foreground="#1976D2"/>
                                </TextBlock>
                            </Border>
                        </WrapPanel>
                        <Border Background="White" CornerRadius="10" Padding="5"
                                BorderBrush="#e2a42d" BorderThickness="2">
                            <DataGrid x:Name="dgvMisVentas" AutoGenerateColumns="False" IsReadOnly="True"
                                      Height="200" Background="White" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="100"/>
                                    <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="150"/>
                                    <DataGridTextColumn Header="Productos" Binding="{Binding CantidadProductos}" Width="80"/>
                                    <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}" Width="100"/>
                                    <DataGridTemplateColumn Header="Acciones" Width="100">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Ver Detalle" Click="btnVerDetalleVenta_Click"
                                                        Background="#2196F3" Foreground="White" Padding="5" FontSize="10"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="📦 PRODUCTOS DISPONIBLES" Margin="0,0,0,20" FontWeight="Bold" Foreground="#a36b00">
                    <StackPanel Margin="10">
                        <Border Background="White" CornerRadius="10" Padding="5"
                                BorderBrush="#e2a42d" BorderThickness="2">
                            <DataGrid x:Name="dgvProductosDisponibles" AutoGenerateColumns="False" IsReadOnly="True"
                                      Height="150" Background="White" BorderThickness="0">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Producto" Binding="{Binding Nombre}" Width="*"/>
                                    <DataGridTextColumn Header="Precio" Binding="{Binding Precio, StringFormat=C}" Width="100"/>
                                    <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80"/>
                                    <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Border>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </Border>
    </Grid>
</Window>